#ifndef AURORA_MESH_H
#define AURORA_MESH_H

#include <malloc.h>
#include <pspgu.h>

#include <Aurora\Graphics\Node.h>
#include <Aurora\Graphics\Vertex.h>
#include <Aurora\Math\Vector3.h>
#include <vector>

using namespace Aurora::Math;

namespace Aurora
{
	namespace Graphics
	{
		/*class Mesh : public Node
		{
		public:

			Mesh();
			~Mesh();
			Mesh(std::string name,Math::Vector3 position);
			Mesh(std::string name,Math::Vector3 position,Math::Vector3 scale);

			//void AddVertex(Vertex vertex);
			void AddFace(Face face);

			void FinishMesh();

			//std::vector<Vertex>		Vertices;
			std::vector<Face>		Faces;

			SimplePSPVertex * 		meshvertices;
		};*/
	}
}

#endif


